version: '3.8'
services:
  noteflow:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    volumes:
      - noteflow_data:/app/notes.db
    restart: unless-stopped

volumes:
  noteflow_data: